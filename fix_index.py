import re

with open("index.html", "r", encoding="utf-8") as f:
    html = f.read()

# I will find the block starting with "???? ?????? ?? ?????? ?????" and ending with the else if
pattern = re.compile(r'"???? ?????? ?? ?????? ????? ?? ????? ???\s*else if \(question\.includes\(\'hello\'\)', re.MULTILINE | re.DOTALL)

fixed = """\"???? ?????? ?? ?????? ????? ?? ????? ????? ?????! ?????? ????? ?? ???? ????? ???????? ????"
          ],
          Bangla: [
            "????????? ????? ??? ???? ?????? ?????? ??????! ???????? ???????????? ????? ?????? ???? ???, ????? ?????? ?????? ??? ???????? ????"
          ]
        };
        return getRandomResponse(responses[language] || responses.English);
      }
      
      // Hello
      else if (question.includes('hello')"""

if pattern.search(html):
    html = pattern.sub(fixed, html)
    with open("index.html", "w", encoding="utf-8") as f:
        f.write(html)
    print("Fixed with regex!")
else:
    print("Regex still failed.")
